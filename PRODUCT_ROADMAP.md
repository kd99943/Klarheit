# Klarheit 产品路线图

> 定位：高端 D2C 眼镜电商平台，核心差异化功能为 AR 虚拟试戴。
> 目标：从当前 Demo 状态推进至可正式发布的生产级产品。

---

## 当前状态评估

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户注册/登录 | ✅ 生产就绪 | JWT 认证正常，IP 速率限制已实现，密码和会话加固完成 |
| 产品目录 | ✅ 生产就绪 | 从后端 API 动态拉取，完整适配 i18n 多语言显示 |
| 配镜向导 | ✅ 生产就绪 | 前端数值实时校验，镜片 API 数据联动，步骤间状态无损传递 |
| AR 虚拟试戴 | ✅ 生产就绪 | 自研 WebAR，基于 MediaPipe Face Landmarker 与 Three.js 叠加，性能及兼容性良好 |
| 结账流程 | ✅ 生产就绪 | 接收配镜向导数据并呈现详情，支持结账单生成，且已集成支付宝/微信收银台接口 |
| 订单管理 | ✅ 生产就绪 | 后端提供 `/my` 历史订单列表，前端渲染真实个人账户数据 |
| 安全与稳定性 | ✅ 已达标 | 密钥迁移环境变量，具 IP 防刷率限制，结构化日志与前后端测试全通过 |
| 支付与营销 | ✅ 生产就绪 | 极简微信/支付宝支付网关与防刷优惠码子系统设计与实现全部完成 (Phase 3) |
| 部署基础设施 | 🔄 进行中 | 完成 Docker 容器化及 docker-compose.yml 一键启动开发，生产环境 HTTPS/CDN 排期中 (Phase 5) |

---

## 阶段规划

### Phase 0 — 地基加固（已完成 ✅）

**目标：** 在添加任何新功能之前，先把现有代码的安全和质量问题修复，避免技术债务在后续阶段放大。

**任务清单：**

#### 安全
- [x] 将 JWT 密钥迁移至环境变量，从代码库中移除硬编码值
- [x] 限制 CORS 白名单，仅允许前端域名
- [x] 对认证接口添加速率限制（防暴力破解）
- [x] 对所有用户输入添加后端校验（处方数值范围、邮箱格式等）

#### 代码质量
- [x] 移除处方表单中的硬编码测试默认值（-2.25 等）
- [x] 统一前端 API 错误处理，添加全局 Error Boundary
- [x] 提取重复的产品加载逻辑为共享 hook（`useProducts`）
- [x] 后端添加结构化日志（SLF4J + Logback）

#### 测试
- [x] 后端：补充核心业务逻辑的单元测试（订单计算、处方校验）
- [x] 前端：为关键组件添加基础渲染测试

#### 数据库
- [x] 引入 Flyway 管理数据库迁移，替代 `schema.sql`

**阶段验收测试：**
- 安全扫描：无硬编码密钥泄露
- 所有现有后端集成测试通过
- 前端无控制台报错，所有页面正常渲染

---

### Phase 1 — 核心用户流程闭环（已完成 ✅）

**目标：** 让用户能完整走完"浏览 → 配镜 → 下单 → 确认"的核心购买链路。

**任务清单：**

#### 配镜向导（Config Lab）修复
- [x] 修复步骤间状态传递（处方数据 → 镜片选择 → 汇总）
- [x] 添加处方数值的前端实时校验（SPH/CYL 范围、AXIS 0-180°）
- [x] 镜片选择步骤与后端 `lens-options` API 真实联动
- [x] 配镜完成后携带完整数据跳转结账页

#### 结账流程完善
- [x] 结账页接收并展示来自配镜向导的完整数据
- [x] 订单提交后展示订单确认页（含订单号、商品摘要、预计交期）
- [x] 接入邮件服务（推荐 Resend 或 SendGrid），发送订单确认邮件

#### 我的账户 — 订单历史
- [x] 后端添加 `GET /api/v1/orders/my` 接口，返回当前用户订单列表
- [x] 前端订单历史页展示真实数据（订单号、状态、金额、日期）
- [x] 订单详情页（点击订单可查看完整信息）

**阶段验收测试：**
- 完整走通注册 → 浏览产品 → 配镜 → 下单 → 收到确认邮件 → 在账户页查看订单
- 处方输入错误值时前后端均有拦截提示

---

### Phase 2 — AR 虚拟试戴（已完成 ✅）

**目标：** 实现核心差异化功能，让用户在购买前能虚拟试戴眼镜。

**技术选型及完成清单 (方案 A: MediaPipe Face Landmarker + Three.js)：**

- [x] 集成 MediaPipe Face Landmarker 实现实时人脸 3D 关键点检测
- [x] 使用 Three.js 将 3D 眼镜模型与姿态矩阵叠加到视频流上
- [x] 实现眼镜位置、旋转和缩放自动对齐 (瞳距与鼻梁定位)
- [x] 摄像头权限管理、动态加载及支持前后摄像头转换
- [x] 切换不同产品时，AR 3D 材质和颜色实时渲染更新
- [x] AR 页面添加截图/本地下载功能
- [x] 完善的降级处理机制（无摄像头、人脸丢失、浏览器不兼容等异常状态友好提示）

---

### Phase 3 — 支付与商业化（已完成 ✅）

**目标：** 接入真实/沙箱支付通道与营销折扣体系，使平台具备闭环交易与促活能力。

**任务清单：**

#### 支付集成
- [x] 确定支付方案（国内主流双通道：支付宝支付 + 微信支付）
- [x] 后端实现支付意向创建接口，返回起调数据（微信二维码/支付宝沙箱 HTML 表单）
- [x] 后端实现支付回调（Webhook）安全验签处理与状态幂等更新
- [x] 前端重构收银台组件，支持微信和支付宝单选切换，并渲染扫码及跳转
- [x] 订单支付成功后，路由跳转至对应的订单确认及状态看板页

#### 订单状态管理与营销
- [x] 完善订单状态流转：`PENDING_PAYMENT` → `PAID` (已付) / 状态控制
- [x] 整合 Coupon/Promo 优惠券校验模块与行锁防高并发超额使用逻辑
- [x] 订单生成并异步发送邮件通知用户（Resend API 接入完毕）

**阶段验收测试：**
- 使用支付宝沙箱 APP 及买家测试账户完成扫码支付，状态流转正常
- 并发使用折扣码超限校验功能符合防刷预期
- 成功获得对应的本地支付凭据与邮件发送指令

---

### Phase 4 — 产品体验提升（持续迭代）

**目标：** 提升产品完整度和用户体验，为正式发布做准备。

**任务清单：**

#### 产品目录增强
- [ ] 产品过滤（按材质、价格区间、镜框形状）
- [ ] 产品搜索
- [ ] 产品详情页（独立 URL，SEO 友好）
- [ ] 产品图片多角度展示

#### 用户体验
- [ ] 收藏/心愿单功能
- [ ] 最近浏览记录
- [ ] 优惠码/折扣功能
- [ ] 用户地址簿（保存常用收货地址）

#### 性能
- [ ] 产品列表接口添加分页
- [ ] 前端添加图片懒加载
- [ ] 后端接口添加缓存（Redis 或 Spring Cache）

---

### Phase 5 — 生产部署（进行中 🔄）

**目标：** 将应用部署到生产环境，完成容器化并具备上线条件。

**任务清单：**

#### 容器化（已完成 ✅）
- [x] 编写 `Dockerfile`（前端 + 后端分别，采用多阶段构建）
- [x] 编写 `docker-compose.yml`（集成 MySQL 并实现本地一键启动）

#### CI/CD
- [ ] 配置 GitHub Actions：代码推送自动运行测试
- [ ] 配置自动构建和部署流水线

#### 生产配置
- [ ] 配置生产环境变量（数据库、JWT 密钥、邮件服务）
- [ ] 配置 HTTPS（SSL 证书）
- [ ] 配置域名和 CDN（静态资源加速）
- [ ] 数据库备份策略

#### 监控
- [ ] 接入错误监控（Sentry）
- [ ] 配置基础性能监控（响应时间、错误率）

**阶段验收测试：**
- 生产环境完整走通核心购买链路
- HTTPS 正常，无混合内容警告
- 错误监控收到测试告警

---

## 优先级总结

```
Phase 0（地基）→ Phase 1（流程闭环）→ Phase 2（AR 核心功能）
     → Phase 3（支付）→ Phase 4（体验提升）→ Phase 5（部署上线）
```

**关键路径：** Phase 0 和 Phase 1 是所有后续工作的前提，必须先完成。Phase 2 是核心差异化，应在支付之前完成，因为 AR 体验是用户选择 Klarheit 而非竞品的核心理由。

---

## 技术债务备忘

以下问题已知但未纳入阶段任务，需在合适时机处理：

- 前端缺少 API 响应类型的完整 TypeScript 定义
- 后端缺少 OpenAPI/Swagger 文档
- 产品数据目前通过 `DataInitializer` 硬编码，后续需要管理后台或 CMS
- 无库存管理系统（当前假设无限库存）

---

*文档创建时间：2026-05-14*
*当前版本：v1.0*
